<!doctype html>
<html>
  <head>
    <title>Welcome to <%=clinicData.clinicName%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1><%=clinicData.clinicName%></h1>
    <h2>Here is the audit history:</h2>
    <table>
    <thead>
        <tr>
            <th>User Name</th>
            <th>Success</th>
            <th>Time</th>
        </tr>
    </thead>
    <tbody>
        <% auditlist.forEach(function(audit) { %>
            <tr>
                <td><%= audit.username %></td>
                <td><%= audit.success? 'Success' : 'Failed' %></td>
                <td><%= new Date(audit.time).toLocaleString() %></td>
            </tr>
        <% }); %>
    </tbody>
    
</table>
<p><a href="/">Back to Home</a></p>
</body>
</html>
