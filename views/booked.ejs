<!doctype html>
<html>
<head>
    <title>Appointment Confirmed <%= clinicData.clinicName %></title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>

<body>
<div class="container">
    <h1>Appointment Confirmed</h1>

    <p>Your appointment has been successfully booked.</p>

    <table>
        <tr>
            <th>Patient</th>
            <td><%= patient.first_name %> <%= patient.last_name %></td>
        </tr>
        <tr>
            <th>Date</th>
            <td><%= date %></td>
        </tr>
        <tr>
            <th>Time</th>
            <td><%= time %></td>
        </tr>
    </table>

   <% if (weather) { %>
    <h2><br>Weather Forecast for <%= date %></h2>

    <p><strong>Temperature:</strong> <%= weather.main.temp %> °C</p>
    <p><strong>Feels Like:</strong> <%= weather.main.feels_like %> °C</p>
    <p><strong>Humidity:</strong> <%= weather.main.humidity %>%</p>
    <p><strong>Wind Speed:</strong> <%= weather.wind.speed %> m/s</p>
    <p><strong>Conditions:</strong> <%= weather.weather[0].description %></p>
    <% } else { %>
    <p>No forecast available for this date (more than 5 days ahead).</p>
    <% } %>


    <a href="/appointments/book">Book Another Appointment</a>
    <a href="/">Return to Home</a>
</div>
</body>
</html>
